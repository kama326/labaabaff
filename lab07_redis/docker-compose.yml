version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: nosql_lab_redis
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
      - ./:/scripts
    command: redis-server --appendonly yes
